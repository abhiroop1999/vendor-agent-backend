{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "GOOGLE_API_KEY=$GOOGLE_API_KEY uvicorn main:app --host 0.0.0.0 --port $PORT"
  }
}
